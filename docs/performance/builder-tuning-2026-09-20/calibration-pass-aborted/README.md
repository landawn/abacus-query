This pass was stopped during baseline fork 5 after the single last warmup sample was found to be an unreliable calibration input. A pause reduced positional_select_entity to only 10 operations per measured sample in current fork 4 (far below the intended ~20 ms). Earlier valid-looking timing medians were also variable. Completed forks and the deliberately interrupted final fork are preserved; no samples from this pass are included in final summaries. The final harness instead calibrates from five-sample medians and increases the batch until its median reaches 2 ms, identically for every version.
