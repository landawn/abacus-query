Exploratory runs, excluded from final summaries. The initial/stage1-stage4 files used 132 cases and -Xms512m -Xmx1024m. The expanded256 and stage6 files use the final 188-case harness and -Xms128m -Xmx256m. Initial retry_rollback fixtures constructed a LinkedHashMap from Map.of, whose iteration order varies across JVMs; the expanded harness inserts its two entries explicitly and removes that confound. Early results drove follow-up work on bean batches, camel-case identifiers, and large IN lists containing expressions. Do not combine these measurements with final/; they include intermediate implementations and different benchmark coverage.
